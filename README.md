# uca_expresiones
## Paso 4 — Verificación final

Corrí `cargo run` y `cargo test` desde `tipos_operandos_rust/` para confirmar que todo funciona correctamente.

Salida de `cargo test`:

```
(pega aquí la salida que copiaste)
```